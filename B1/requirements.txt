# Military Hierarchy Voice Relay System - Requirements
# Install with: pip install -r requirements.txt

# Core Discord library with voice support
discord.py[voice]>=2.3.2,<3.0.0

# Voice receiving extension (critical for capturing commander audio)
discord-ext-voice-recv>=0.5.0a167

# Encryption for Discord voice
PyNaCl>=1.5.0

# Speech recognition for voice triggers (optional but recommended)
SpeechRecognition>=3.10.0
